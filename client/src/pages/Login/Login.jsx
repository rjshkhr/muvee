import Auth from '../../components/Form/Auth'

import * as Styled from './Login.styles'

const Login = () => {
  return (
    <Styled.Container>
      <Auth label='log in' />
    </Styled.Container>
  )
}

export default Login
