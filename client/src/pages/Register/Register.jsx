import Auth from '../../components/Form/Auth'

import * as Styled from './Register.styles'

const Register = () => {
  return (
    <Styled.Container>
      <Auth label='register' />
    </Styled.Container>
  )
}

export default Register
